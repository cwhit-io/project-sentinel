# Project Status

**Status: disposable Zabbix core/transport and sealed OpenBao compatibility milestones independently passed; one exact PostgreSQL restore remains operator/validator-attested but not durably accepted, and restored-application validation failed on evidence handling. Production acceptance remains blocked.**

## Current phase

On 2026-07-28 the user explicitly approved expediting a local commissioning beta so containers can be started for login and testing. This authorization is limited to a fresh, synthetic-only PostgreSQL/Zabbix core plus the disposable lab agent, with loopback-only publication. It does not authorize OpenBao startup or initialization, backup execution, real credentials or targets, monitoring apply, StackStorm, remediation, or production use. A pre-start check passed for fresh namespace `sentinel-beta-20260728`, absent project resources, free loopback ports `18080` and `11051`, absent repository `.env`, and absent backup lock. The synthetic database value is to be generated only within the launch process, never printed or written to disk; privileged Docker metadata can contain it while the disposable containers exist.

The bounded disposable Zabbix core/transport milestone independently passed reviewer audit. PostgreSQL, Zabbix server, Zabbix web, and the synthetic agent were healthy with restart count 0; web/API and server publication were loopback-only, API version was `7.0.14`, server-to-agent `agent.ping` was `1`, and a host-originated agent request was rejected. PostgreSQL and the agent had no host publication. No agent registration, dashboard/data validation, alerts, authenticated API operation, or monitoring apply occurred.

The exact second disposable PostgreSQL restore at `2026-07-28T05:21:14Z`–`05:21:21Z` is an operator/validator-attested demonstration of the clean options and exit `0`, not durable acceptance. The live, bounded selected aggregates from source container `2a3be4a24a953cd4f3e0db5f8d79698e2726af4107a275061e2551522038e269` in project `sentinel-evidence-zabbix` and the network-none destination matched at `203|392|17659|6718|7000000`; this is not complete database equivalence. The evidence remains uncommitted and the transient runtime events have expired. The earlier `2026-07-28T05:18:35Z`–`05:18:41Z` execution is operator-recorded but not independently or durably verified. The second execution was an in-place clean-option rerun into that same mutable destination, not an independent fresh restore, so generalized repeatability remains blocked. The direct-pipe/no-dump account is likewise operator-recorded and cannot be independently reconstructed because no dump artifact was retained.

Restored-application technical observations recorded a fresh isolated internal network, the selected aggregate above on the restored database, a ready Zabbix server process, internal web HTTP `200`, and no published ports. The independent validator run is **FAILED** because it used unrestricted `docker inspect` and exposed synthetic environment fields in private output. Values are intentionally not repeated. This is an evidence-handling incident: the restored-application milestone is not accepted and requires a future safe, field-scoped revalidation.

For `sentinel-night-openbao2`, effective publication was exactly `127.0.0.1:18200->8200`. A CA-verified loopback health request returned HTTP 200, while a non-loopback host connection was refused. Networks were exactly the dedicated non-internal `sentinel-night-openbao2-openbao-operator` and internal `sentinel-night-openbao2-secrets`, with one member each. The effective container controls were numeric user `0:0`, capability drop `ALL`, capability add only `DAC_READ_SEARCH` and `IPC_LOCK`, read-only root, no-new-privileges, read-only config/policy/TLS binds, expected named data/audit volumes, and bounded tmpfs mounts.

No initialization, unseal, authentication, bootstrap helper, credentials, or recovery material were used. The fresh project was fully torn down and TLS was preserved. This is sealed compatibility evidence only, not bootstrap or production acceptance. Numeric root with the broad `DAC_READ_SEARCH` capability and ordinary bridge NAT remain production blockers.

All disposable source, restore, and application containers; their project networks and named volumes; and the explicitly inventoried anonymous volumes were removed. Unrelated volumes and images and commissioning TLS were preserved. No real credentials or recovery material were used.

The current evidence records the independently reviewer-passed disposable Zabbix core/transport boundary, sealed OpenBao compatibility, an operator/validator-attested exact PostgreSQL restore demonstration, and a live/bounded selected-aggregate match. The restore is not durably accepted, and the aggregate is not complete database equivalence. The failed restored-application validation does not establish accepted application recovery. It does not establish restore repeatability, registration, dashboard/data validation, alert delivery, StackStorm receipt, OpenBao bootstrap, encrypted backup or production recovery capability, image authenticity/provenance, or end-to-end acceptance. Treat all procedures as protected commissioning runbooks, not as production evidence.

See `docs/commissioning-report.md` for the evidence and verification matrix. Do not enroll real credentials, apply changes, or declare deployment complete until the blocked and not-started checks have been completed and reviewed.

## Completed evidence

- Repository and scoped `AGENTS.md` guidance inspected.
- Existing worktree changes preserved; no production services or targets started.
- Read-only reviewer identified plan/apply structure, export sanitization, planning validation, and generated-catalog contract gaps.
- Read-only validator confirmed available static checks and Compose interpolation; host pytest and shellcheck remain unavailable, while external disposable pytest and yamllint paths are available.
- Planner now performs complete validation, binds verified plans to current desired state, reports malformed/unmanaged observed hosts, and keeps apply mutation-disabled.
- Catalog generation validates inputs, writes the generated artifact deterministically, and escapes rendered metadata.
- Generated dry-run plan was regenerated with `source: desired-state` and independently verified without mutation.
- Historical final disposable validation passed compilation, YAML parsing, shell syntax, validation, catalog/plan generation, deterministic planner checks, Compose interpolation, a tracked-file-only secret-pattern scan, and diff checks. Ignored commissioning TLS contents were not inspected, and dedicated secret-scanning tools remain unavailable.
- Historical pre-runtime inspection found no resources for the earlier `sentinel-runtime` project; the later `sentinel-openbao-commissioning` resources are separately inventoried below.
- Disposable core runtime evidence: PostgreSQL, Zabbix server, and Zabbix web reached healthy container states; PostgreSQL accepted local readiness checks, the Zabbix server process was present, and the web container returned HTTP 200 internally.
- Historical evidence: disposable developer tools were installed outside the repository under `/tmp/opencode`; the then-current external-path pytest suite passed 20 tests.
- Teardown verification found no `sentinel-runtime` containers and no explicitly named `sentinel-*` networks or volumes; the ephemeral synthetic environment file was removed.
- Added a paused OpenBao preflight that checks operator-provided TLS metadata, ownership/modes, SAN, key match, CA chain, Docker availability, namespace/project collisions, and fails without initialization or file creation.
- Added a `lab` Compose profile with a monitoring-only synthetic Zabbix agent target, internal network scope, no host port, resource limits, and health gating.
- Added `docs/commissioning-lab.md` with phase gates, evidence requirements, teardown, corrected-preflight/TLS metadata evidence, and explicit blocked/not-tested status for sealed startup and later phases.
- Historical preparation validation: the then-current 20-test disposable-path pytest suite passed with shell syntax, compilation, YAML parsing, Compose base/lab configuration, safe missing-TLS preflight failure, and diff checks.
- Prepared `scripts/openbao-bootstrap-commissioning.sh` as a two-phase, guarded local helper: `init` directly attaches 3-share/2-threshold output to the operator terminal and exits; `configure` requires an externally loaded bootstrap token, configures audit/KV/policies/AppRoles, and attempts root-token revocation. The helper was not executed.
- Historical static disposable OpenBao redesign evidence: Compose rendered `DAC_READ_SEARCH` without `DAC_OVERRIDE`, retained all specified hardening, and accepted restrictive tmpfs overrides for `/openbao/file` and `/openbao/logs`; the then-current focused test file passed 25 tests. This is not runtime evidence.
- The OpenBao healthcheck now statically accepts `bao status` exit `0` (unsealed) or `2` (sealed/uninitialized but reachable), while other exits fail; it retains HTTPS CA verification and proves API/TLS reachability only, not initialization or acceptance. Runtime execution remains blocked/not tested.
- Historical post-cleanup correction evidence: external pytest passed 31 tests; helper shell syntax, test compilation, commissioning-record consistency, and scoped diff checks passed. Those were static checks only.
- Approved stale-resource cleanup removed exactly one exited project container, one project network, two named project volumes, and two anonymous volumes attached to that container: container `sentinel-openbao-commissioning-openbao-1`, network `sentinel-openbao-commissioning-secrets`, named volumes `sentinel-openbao-commissioning-openbao-data` and `sentinel-openbao-commissioning-openbao-audit`, and the two recorded anonymous volume IDs; the TLS host bind was preserved.
- Post-cleanup verification found project containers, networks, and named volumes absent. The exact prior anonymous volume IDs `f217b92076e00129830d5f2c94d8603bdda55d7baf15c91ca901241a4aaa2b38` and `ee87fa7623506bec8657f2f02f88ae364757b0da3b2eebdaee38fcdbbcd5309f` are absent; commissioning TLS metadata remains.
- Corrected exact-path no-init preflight passed after cleanup for `sentinel-openbao-commissioning`; no files were created and no OpenBao service was contacted.
- A later approved sealed OpenBao start succeeded internally: OpenBao remains sealed and uninitialized, with internal TLS/API reachability but no effective host publication. No initialization, unseal, authentication, helper execution, or recovery-material generation occurred.
- The current static networking definition has non-internal `${SENTINEL_NAMESPACE}-openbao-operator` for OpenBao only and `${SENTINEL_NAMESPACE}-zabbix-operator` for Zabbix server/web only. PostgreSQL and the synthetic agent stay off operator bridges. The invalid monolithic StackStorm service, automation network/profile, port setting, and StackStorm operator network were removed without substituting a partial component stack. StackStorm contracts remain inert, every notification route is schema-required to set `enabled: false`, and notification receipt/delivery is blocked. Policy references to those disabled route IDs remain valid desired intent.
- Every runnable Compose image now uses a readable tag locked to its supplied tested local repository digest. An operator attested that a bounded registry check resolved all five exact configured `name:tag@sha256` references at that point in time. That resolution is not durably accepted or independently retained pull evidence. It is not publisher identity, signature, attestation, provenance, continuing availability, runtime health, or production acceptance.
- Historical bounded static-correction evidence: the external-path pytest suite passed 35 tests and all-profile Compose rendering passed with synthetic disposable interpolation values. Those static checks did not establish runtime behavior.
- The no-init/start runbook now binds both steps to the reviewed physical checkout: the operator sets `ROOT=$(pwd -P)` at that repository root, invokes `"$ROOT/scripts/openbao-preflight.sh"`, and starts with `--project-directory "$ROOT" -f "$ROOT/compose.yaml" --project-name "$COMPOSE_PROJECT_NAME" --env-file /dev/null`. The preflight independently resolves its physical root from its script path. Generic relative startup examples are prohibited.
- Preflight requires equal namespace/project names matching Docker Compose-compatible grammar: lowercase alphanumeric first, followed only by lowercase alphanumeric, underscore, or hyphen. Empty values, uppercase, dots, leading punctuation, and spaces are rejected.
- Completed sealed compatibility evidence: old project `sentinel-overnight-openbao` was verified uninitialized/sealed and its exact internal-only container, network, and named data/audit volumes were removed; TLS was preserved.
- Fresh project `sentinel-night-openbao2` passed preflight, started with the corrected topology, and was observed running healthy with restart count 0; independent status was `initialized=false`, `sealed=true`.
- Effective publication was exactly `127.0.0.1:18200->8200`; CA-verified loopback health returned HTTP 200 and a non-loopback host connection was refused.
- Network inspection showed exactly `sentinel-night-openbao2-openbao-operator` (non-internal) and `sentinel-night-openbao2-secrets` (internal), each with exactly one member.
- Effective hardening inspection confirmed `user 0:0`, `cap_drop: ALL`, only `DAC_READ_SEARCH` and `IPC_LOCK` added, read-only root, no-new-privileges, read-only config/policy/TLS binds, expected named data/audit mounts, and bounded tmpfs mounts.
- No initialization, unseal, authentication, helper, credentials, or recovery material occurred. The fresh project was fully torn down and TLS was preserved.
- PostgreSQL restore evidence records source container `2a3be4a24a953cd4f3e0db5f8d79698e2726af4107a275061e2551522038e269`, the existing destination container/volume, and destination network mode `none`.
- The exact second clean-option restore at `2026-07-28T05:21:14Z`–`05:21:21Z` is an operator/validator-attested demonstration of exit `0`; a live/bounded source and destination selected-aggregate comparison matched at `203|392|17659|6718|7000000`, but does not establish complete database equivalence.
- This restore demonstration is not durably accepted: its evidence remains uncommitted and its transient runtime events have expired. The first `2026-07-28T05:18:35Z`–`05:18:41Z` execution and direct-pipe/no-dump account remain operator-recorded, not independently or durably verifiable. The demonstrated rerun used one mutable destination, not a fresh destination; generalized repeatability remains blocked.
- Independently reviewer-passed Zabbix core/transport evidence records four healthy/restart-0 containers, loopback-only web/API and server publication, API `7.0.14`, server-to-agent ping `1`, host-to-agent rejection, and no PostgreSQL or agent publication. Registration, dashboards, alerts, authentication, and apply were not performed.
- Restored-application technical checks were observed on a fresh isolated internal network, but the independent validator run **FAILED** after unrestricted inspection exposed synthetic environment fields in private output. No values are repeated; the milestone is not accepted pending safe field-scoped revalidation.
- Final cleanup removed all disposable source/restore/application containers, project networks, named volumes, and explicitly inventoried anonymous volumes while preserving unrelated volumes/images and commissioning TLS.
- Current bounded corrections reject duplicate JSON object members at every plan depth and independently pin the StackStorm duplicate-member parser/order plus atomic reserve-if-absent, shared-linearizable, fail-closed, restart-persistent replay-store contract. These remain inert desired-state validation; no handler or runtime store exists. The backup script now uses `setsid timeout` and Bash `pipefail` for bounded `pg_dump | age`, `tar | age`, and manifest encryption; tracks session PID plus Linux `/proc` starttime and PGID; reaps before staging/lock cleanup; rejects missing/partial stale-lock metadata for escalation; and uses validated Python `hashlib` checksums. It retains exact dump options, exact nonempty publication layout, fsync/no-replace publication, preserve-all-sets behavior, and blocked runtime claims. Mocked tests used no age identity, credentials, Docker service, or runtime backup. The external dependency-path full suite passed **82 tests** and focused backup selection passed **18 tests**. Bash syntax, `sentinel validate`, external yamllint, tracked-file detect-secrets (**0 findings** with generated-plan `integrity` lines excluded by key), scoped diff checks, and pinned ShellCheck-alpine `v0.10.0` for `scripts/backup.sh` passed. No plaintext secret value was printed or reviewed. No backup or remediation ran.

## Blockers

- Live Zabbix registration/reconciliation, dashboard/data validation, alerting, OpenBao bootstrap, StackStorm deployment/receipt, image authenticity verification, encrypted backup, restore repeatability, accepted restored-application startup, and production recovery remain untested, failed, or blocked. The exact second disposable PostgreSQL restore is only an operator/validator-attested, non-durably-accepted demonstration, and the selected-aggregate match is live/bounded rather than complete database equivalence.
- Host ShellCheck is unavailable, but pinned-container ShellCheck passed the backup script. The external dependency-path pytest suite passed 82 tests. These static results cannot establish runtime webhook enforcement, filesystem durability, decryptability, recoverability, or backup behavior.
- No applicable plan creation path exists. Runtime integration, identity-bound signed approval, and live/offsite backup behavior remain blockers outside this safe local hardening pass.
- Export remains intentionally non-operational; the sanitizer is not evidence of live export safety.
- All five exact configured image references have operator-attested point-in-time registry resolution and remain digest-pinned, but that resolution is not durably accepted. Publisher identity, provenance, signature, attestation, continuing registry availability, and image security verification remain blocked.
- OpenBao bootstrap remains paused; no recovery material will be generated or recorded by this coordinator.
- Disposable synthetic-agent transport evidence was established, but no registration, monitoring data, dashboard, StackStorm, or alert evidence exists. PostgreSQL recovery evidence is limited to one operator/validator-attested exact timestamped restore demonstration and a live/bounded selected-aggregate match; generalized repeatability remains blocked. OpenBao evidence is limited to sealed compatibility.
- Encrypted artifacts, age identity/custody, manifest/offsite handling, roles/global objects, PITR, accepted application startup from the restored database, and production recovery remain blocked. The prior restored-application validator run failed evidence handling and cannot support acceptance.
- OpenBao bootstrap cannot be completed in this coordinator session because it would require handling recovery material; dev mode is prohibited. A protected recovery-material workflow is required.
- A disposable target/API identity and complete StackStorm dependency stack are not defined sufficiently for safe end-to-end alert testing; Compose intentionally provides no runnable StackStorm substitute.
- Commissioning-only TLS material is operator-provided; the corrected exact-path preflight passed and production reuse is prohibited.
- The OpenBao pause gate remains human-controlled; no command initialized, unsealed, authenticated to OpenBao, or ran the helper. The completed sealed startup established bounded compatibility and loopback publication, not bootstrap or acceptance.
- No-init preflight run with unique namespace `sentinel-preflight-20260728` failed closed as expected: TLS directory/material is absent and the repository config/policy directories have permissions the preflight rejects. No files or runtime resources were created.
- Historical evidence: after the operator supplied commissioning-only TLS material, the config/policy directory write permissions were tightened locally and the then-current override-capable TLS preflight passed for namespace/project `sentinel-openbao-commissioning`. That historical result is superseded by the recorded post-cleanup corrected exact-path preflight pass. OpenBao remains uninitialized; no recovery material was generated.
- The earlier TLS-access blocker was corrected without weakening the operator-owned mode-`0700` TLS directory or mode-`0600` key. Compose adds `DAC_READ_SEARCH` alongside `IPC_LOCK` and does not add `DAC_OVERRIDE`; the sealed start reached internal TLS/API success. `DAC_READ_SEARCH` remains a broad process-wide capability accepted only for isolated commissioning and prohibited for production. This is not production hardening or runtime acceptance.
- The image-declared anonymous writable volumes at `/openbao/file` and `/openbao/logs` are now statically overridden with bounded mode-`0700`, `nosuid,nodev,noexec` tmpfs mounts. Configured storage and audit remain on the unchanged named `/openbao/data` and `/openbao/audit` mounts. Compose rendering is static evidence only.
- Runtime evidence covered only the OpenBao service's dedicated operator/secrets topology, loopback success, and one non-loopback host refusal. Ordinary Docker bridge NAT and broader host-routed, Docker-gateway, LAN, Internet, firewall, and egress behavior remain production blockers. Numeric root plus broad `DAC_READ_SEARCH` is also prohibited for production acceptance.

## Next safe action

Under the bounded user authorization recorded above, render the exact Compose configuration without emitting environment values, then start only `postgres`, `zabbix-server`, `zabbix-web`, and `synthetic-zabbix-agent` in project `sentinel-beta-20260728`. Verify health, restart counts, exact loopback publication, web HTTP/API reachability, and synthetic transport using field-scoped commands that cannot emit environment fields. Do not start OpenBao, initialize or unseal it, run either helper phase, handle recovery material, execute a backup, apply monitoring changes, connect real targets, or enable StackStorm/remediation. Backup remediation remains outside this beta launch and unaccepted.
