# Disposable commissioning lab

This record is non-operational and synthetic-only. The completed bounded milestone is the Zabbix core/transport, public ingress, mocked reconciliation v3, and read-only discovery gate. Real infrastructure, credentials, secret values, recovery material, and production remediation remain prohibited. For the canonical boundary, see [`docs/scope.md`](scope.md): OpenBao bootstrap, encrypted PostgreSQL backup, restore, and production recovery are out of scope per `docs/scope.md`.

The independent Zabbix core/transport milestone separately passed reviewer audit: PostgreSQL, server, web, and synthetic agent were healthy at restart count 0; web/API and server were loopback-only; API version was `7.0.14`; server-to-agent `agent.ping` was `1`; host-to-agent access was rejected; and PostgreSQL and the agent were not published. Registration, dashboards/data, alerts, authenticated API use, and apply were deliberately not performed. The Cloudflare tunnel terminates TLS for `sentinel.bhm.li` and forwards plain HTTP to the local Zabbix web at `10.10.97.18:18080`; per `docs/scope.md`, this tunnel and TLS termination are operator-managed and are not a Sentinel responsibility.

The mocked-only Zabbix reconciliation v3 milestone records 42 focused tests passing under the prior external dependency path: closed semantic desired/snapshot/ownership/scope/operation contracts, deterministic create/update/quarantine pure-state simulation, in-memory receipt validation, hard-disabled receipt persistence, and hard-disabled delete eligibility. The protected-live gate static evidence adds a disposable loopback JSON-RPC stub with bounded provider/transport/response/parse negatives, exact-type boundaries, internally derived target binding, fail-closed pre-directory Sentinel-only tag persistence, and exploding-probe approval/executor denials. See `docs/evidence/reconciliation-v3-static-20260728.md` and `docs/evidence/reconciliation-live-gate-static-20260728.md`. Runtime acceptance is out of scope per `docs/scope.md`.

## Staged phases
1. **Zabbix core/agent transport — INDEPENDENT REVIEW PASS.** Four healthy/restart-0 containers, loopback web/API/server, API `7.0.14`, server-to-agent ping `1`, host-agent rejection, and no database/agent publication were recorded. This does not include registration or monitoring apply.
2. **Public ingress beta (Cloudflare) — bounded operator note.** A Cloudflare tunnel terminates TLS for `sentinel.bhm.li` and forwards plain HTTP to the local Zabbix web at `10.10.97.18:18080`. The web container is bound to the LAN address so the tunnel always has a deterministic non-loopback target. The Zabbix `Admin` password was changed by the operator before this URL was reachable. No agent recorded that value. This is a commissioning ingress only, not a production ingress; per `docs/scope.md`, rate limiting, MFA, real credentials, monitoring apply, OpenBao, StackStorm, and encrypted backup remain operator-managed.
3. **Mocked reconciliation v3 — bounded static/mock pass; live blocked.** Exact Zabbix API `7.0.14` is pinned. Bounded scope/name identity queries precede full normalization, unrelated payloads are ignored, and desired-name collisions remain blocking. Closed receipt/result fields and strict per-field validation are static. Receipt persistence and deletion eligibility reject before pathname/artifact parsing; no age or identity linkage is established. The separate client and v3 simulator remain inert exact fakes. The tracked plan is desired-only dry-run evidence.
4. **Read-only discovery gate — static/disposable read-only gate; execution blocked.** HTTPS-default transport and literal-loopback commissioning HTTP were exercised only with a local stub. The exact read boundary allows only four discovery methods and derives its binding internally. Closed artifacts are semantically revalidated, and any malformed or non-Sentinel tag blocks the complete bundle before its path exists. Approval and execution deny before touching inputs; no available mutation policy, mutation client/write handle, credential CLI, or approval acceptance exists.
5. **Dashboard/data verification — OUT OF SCOPE per `docs/scope.md`.** Not part of this Sentinel milestone; downstream integration work is operator-managed.
6. **Notification-only StackStorm receipt — OUT OF SCOPE per `docs/scope.md`.** Compose has no runnable StackStorm service.
7. **Alert recovery, PostgreSQL restore, restored application, encrypted backup, production recovery — OUT OF SCOPE per `docs/scope.md`.** These are operator-managed concerns outside Sentinel.

## Acceptance evidence

Acceptance for what Sentinel owns requires phase-linked timestamps, reviewed command records without secret output, Compose/profile and network scope, health checks, Zabbix core/agent transport evidence, mocked reconciliation v3 static evidence, and the read-only discovery gate evidence. Missing tools, unavailable runtime, skipped checks, or unverified claims remain blocked—not passes. See `docs/scope.md` for the boundary between Sentinel and operator-managed acceptance concerns.

Do not connect to production or external hosts, register real agents, use real credentials or secrets, initialize OpenBao, generate recovery material, or enable remediation.
