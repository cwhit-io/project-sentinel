# Commissioning Report

Date: 2026-07-28

This is a sanitized local validation and disposable runtime report for a commissioning baseline. The project remains non-operational and not production-ready. A generated synthetic database secret and operator-provided commissioning-only TLS material were used locally; TLS contents were not inspected or recorded. No real infrastructure credentials, recovery material, or tokens were requested or exposed. No production services or monitoring targets were started.

## Evidence Recorded

- `python3 scripts/sentinel.py validate`: inventory and policy schemas passed; one synthetic lab asset validated.
- `python3 scripts/sentinel.py catalog`: deterministic catalog regenerated at `docs/monitoring-catalog.md`.
- `python3 scripts/sentinel.py plan --dry-run`: sanitized plan regenerated at `monitoring/exports/plan.json`; it contains only the synthetic asset and no credential references or absolute paths.
- `env SENTINEL_NAMESPACE=sentinel-static COMPOSE_PROJECT_NAME=sentinel-static POSTGRES_USER=zabbix POSTGRES_PASSWORD=<synthetic> POSTGRES_DB=zabbix docker compose --env-file /dev/null --project-directory . -f compose.yaml --project-name sentinel-static --profile secrets config --quiet`: Compose syntax and interpolation passed with matching namespace/project semantics. The placeholder value was synthetic and was not stored.
- `python3 -m compileall -q automation scripts tests`: Python compilation passed.
- `sh -n scripts/openbao-preflight.sh scripts/backup.sh`: shell syntax passed.
- A historical tracked-file-only scan found no dev-root-token configuration, private-key material, or secret values in that limited scope. Ignored commissioning TLS files exist in the workspace; their contents were not inspected. Expected `secret://` references are opaque inventory metadata and are excluded from plans.
- Disposable runtime: `postgres`, `zabbix-server`, and `zabbix-web` started under Compose project `sentinel-runtime` and reached healthy container states. PostgreSQL readiness, the Zabbix server process, and internal web HTTP 200 were verified. No live apply or target registration occurred.
- Historical disposable test run: tools installed outside the repository under `/tmp/opencode`; `PYTHONPATH=/tmp/opencode/sentinel-python-tools python3 -m pytest -q` passed 20 tests after the paused-lab preparation checks were added.
- The current external dependency-path suite passed **82 tests**; focused backup tests passed **18 tests**. The bounded backup correction adds fixed `setsid timeout` process-group bounds with Bash `pipefail`, PID/starttime/PGID identity verification, fail-closed partial/stale-lock adjudication, ordered wait/reap cleanup, Python `hashlib` checksum validation, zero-byte producer rejection, rollback-verification failure, manifest-metadata validation, exact `pg_dump` arguments, atomic no-replace publication failure/collision, and retention/prune override rejection. Bash syntax, `sentinel validate`, external yamllint, tracked-file detect-secrets (0 findings with the generated plan `integrity` line excluded by key), and scoped diff checks passed. Host ShellCheck is unavailable. The pinned local ShellCheck-alpine `v0.10.0` container passed `scripts/backup.sh` with no network and a read-only checkout mount. HTTP framing, signatures, and replay checks are contract/vector tests only, not runtime enforcement. No service was started, receipt written, monitoring state applied, webhook handler supplied, runtime backup performed, or runtime enforcement tested.
- Post-cleanup verification found the `sentinel-openbao-commissioning` project containers, networks, and named volumes absent. The exact prior anonymous volume IDs `f217b92076e00129830d5f2c94d8603bdda55d7baf15c91ca901241a4aaa2b38` and `ee87fa7623506bec8657f2f02f88ae364757b0da3b2eebdaee38fcdbbcd5309f` are absent; commissioning TLS metadata remains.
- The corrected physical-script, exact-path preflight passed after cleanup for matching namespace/project `sentinel-openbao-commissioning`. It checked only `private/openbao/tls/`, rejected every `OPENBAO_TLS_DIR` override, created no files, and contacted no OpenBao service.
- A later approved OpenBao sealed start succeeded internally: OpenBao remains sealed and uninitialized, and the internal TLS/API check passed. No initialization, unseal, authentication, helper execution, bootstrap, or recovery-material generation occurred. The configured loopback `PortBinding` did not become an effective host-published port while the service was attached only to an internal network.
- The current networking definition splits operator access into namespace-scoped, non-internal `openbao-operator` (OpenBao only) and `zabbix-operator` (Zabbix server/web only). Existing internal service networks and loopback bindings remain, while PostgreSQL and the synthetic agent stay off operator bridges. There is no shared operator Docker bridge and no StackStorm service, automation network/profile, or StackStorm operator bridge. The ordinary Docker bridge NAT risk remains accepted only for synthetic commissioning; this does not verify host-routed, Docker-gateway, LAN, Internet, firewall, or egress behavior.
- All five runnable Compose images use readable tags locked to the supplied tested local repository digests. An operator attested that each exact configured `name:tag@sha256` reference resolved in a bounded registry check at that point in time; this is not durably accepted or independently retained pull evidence. Publisher identity, signature, attestation, provenance, continuing availability, runtime health, and production verification remain blocked; see `docs/images.md`.
- Every notification route now explicitly sets `enabled: false`, as required by its commissioning schema. Existing policy references remain valid desired intent, while notification delivery is blocked. The StackStorm contracts are closed and negative-mutation tested as inert documents only; no runtime enforcement is claimed.
- The old internal-only project `sentinel-overnight-openbao` was verified uninitialized and sealed. Its exact container/network/data-and-audit-volume inventory was then removed, while TLS was preserved.
- Fresh project `sentinel-night-openbao2` passed preflight and started with the corrected topology. It was running healthy with restart count 0; independent status reported `initialized=false` and `sealed=true`.
- Effective publication was exactly `127.0.0.1:18200->8200`. A CA-verified loopback health request returned HTTP 200; a non-loopback host connection was refused.
- Its networks were exactly dedicated non-internal `sentinel-night-openbao2-openbao-operator` and internal `sentinel-night-openbao2-secrets`, each with one member. Effective controls were `user 0:0`, `cap_drop: ALL`, only `DAC_READ_SEARCH` and `IPC_LOCK` added, read-only root, no-new-privileges, read-only config/policy/TLS binds, expected named data/audit volumes, and bounded tmpfs.
- No initialization, unseal, authentication, helper, credentials, or recovery material occurred. `sentinel-night-openbao2` was fully torn down and TLS was preserved. This is sealed compatibility evidence only; root plus broad capability and ordinary bridge NAT remain production blockers.
- Any future OpenBao start or bootstrap requires separate approval; this milestone authorizes neither.
- The documented clean start now requires `ROOT=$(pwd -P)` at the reviewed repository root, invokes the preflight through `"$ROOT/scripts/openbao-preflight.sh"`, and binds Compose with `--project-directory "$ROOT" -f "$ROOT/compose.yaml" --project-name "$COMPOSE_PROJECT_NAME" --env-file /dev/null`. The preflight independently resolves its physical checkout root. Generic relative startup examples are rejected.
- Preflight requires equal namespace/project values with Docker Compose-compatible grammar: lowercase alphanumeric first, followed only by lowercase alphanumeric, underscore, or hyphen. Empty values, uppercase, dots, leading punctuation, and spaces are rejected.
- Teardown verification found no containers named `sentinel-runtime-*`, and no explicitly named `sentinel-database`, `sentinel-application`, `sentinel-monitoring`, `sentinel-secrets`, or `sentinel-automation` networks or `sentinel-runtime_*` volumes; the ephemeral synthetic environment file was removed.
- Disposable PostgreSQL restore evidence: source container `2a3be4a24a953cd4f3e0db5f8d79698e2726af4107a275061e2551522038e269` belonged to project `sentinel-evidence-zabbix`; the destination container/volume recorded in `docs/evidence/postgres-restore-20260728.md` used network mode `none`.
- The exact second restore at `2026-07-28T05:21:14Z`–`05:21:21Z` is an operator/validator-attested demonstration of the clean options and exit `0`. A live/bounded selected-aggregate comparison matched source and destination at `203|392|17659|6718|7000000`; it is not complete database equivalence.
- The demonstration is not durably accepted because this evidence is uncommitted and its transient runtime events have expired. The first `2026-07-28T05:18:35Z`–`05:18:41Z` execution is operator-recorded, not independently or durably verified. The demonstrated second execution was an in-place clean-option rerun into one mutable destination, not an independent fresh restore; generalized repeatability remains blocked. The direct `pg_dump -Fc` to `pg_restore` pipe and the account that no dump file was retained are also operator-recorded and cannot be independently reconstructed after execution. No password value was included in the evidence or recorded dump output; synthetic database credentials existed in the disposable container environment while it was running.
- The Zabbix core/transport milestone independently passed reviewer audit: all four containers were healthy at restart count `0`; web/API and server were loopback-only; unauthenticated API version was `7.0.14`; server-to-agent `agent.ping` was `1`; host-to-agent access was rejected; and PostgreSQL and the agent had no publication. No registration, dashboard/data verification, alerting, authenticated API action, or monitoring apply occurred.
- Restored-application technical checks observed a fresh isolated internal network, the restored selected aggregate, a ready server process, internal web HTTP `200`, and no published ports. The independent validator run is **FAILED** because unrestricted `docker inspect` exposed synthetic environment fields in private output. Values are not repeated. This handling incident invalidates milestone acceptance and requires a future safe, field-scoped revalidation.
- Final cleanup removed all disposable source, restore, and application containers, project networks, named volumes, and explicitly inventoried anonymous volumes. Unrelated volumes/images and commissioning TLS were preserved. No real credentials or recovery material were used.

## Failed or Blocked

- With the existing external dependency path, the current suite collected and passed all 82 tests. This is static evidence only, not runtime acceptance.
- Restored-application independent validation failed due to unsafe evidence handling. The technical observations are not an accepted milestone and must be safely revalidated without unrestricted environment inspection.

## Blocked

- Isolated dependency setup: host `pip`/`pytest` remain unavailable; disposable packages were installed outside the repository under `/tmp/opencode` and passed the suite there. No privileged package installation was attempted.
- Image authenticity/signature/provenance verification remains blocked. The supplied tested local repository digests are now immutable Compose pins but were not independently authenticated; see `docs/images.md`.
- Runtime commissioning: the independently reviewer-passed disposable Zabbix core/transport boundary and an operator/validator-attested demonstration of one exact PostgreSQL restore plus a live/bounded selected-aggregate match were recorded, but the restore is not durably accepted and repeatability remains blocked. Restored-application technical observations exist, but independent validation failed on evidence handling, so accepted application startup cannot be claimed. The corrected-topology OpenBao run established only bounded sealed compatibility. No registration/dashboard milestone, alert firing/recovery, StackStorm receipt, idempotent live reconcile, encrypted backup, bootstrap, production recovery, or production acceptance claim can be made.
- OpenBao bootstrap/unseal: intentionally not performed because protected recovery-material delivery cannot be guaranteed in this local session. Follow `docs/openbao.md` in a protected operator environment.
- Production network acceptance remains blocked. The disposable run verified loopback success and one non-loopback host refusal only; ordinary bridge NAT and broader firewall/egress, host-routed, Docker-gateway, LAN, and Internet behavior remain unverified.

## Verification Matrix

| Area | Status | Evidence or gap |
|---|---|---|
| Inventory and policy schema validation | tested locally | Recorded validation passed for one synthetic lab asset. |
| Deterministic catalog generation | tested locally | Recorded catalog generation passed. |
| Review-required dry-run plan and secret/path exclusion | tested locally | Recorded plan contains one synthetic asset and no credential references or absolute paths. |
| Reconciliation planner and apply guard | implemented/static-tested | All generated and verified artifacts are `dry-run`; no applicable format exists. Apply is unconditionally disabled before parsing, receipt generation, or adapter access, including with `--approve` and recomputed integrity. Rollback remains verification/reporting-only for dry-run evidence. Identity-bound signed approval is blocked and not implemented. |
| Compose syntax and interpolation | tested locally | Recorded `docker compose config --quiet` passed with synthetic environment values; disposable core services were separately started and removed. |
| Python and shell syntax | tested locally | Recorded `compileall` and `sh -n` passed. |
| Automated pytest suite | static pass | External dependency path collected and passed 82 tests; focused backup selection passed 18. |
| Immutable image references and registry resolution | operator-attested point-in-time resolution / statically pinned | Every runnable image uses a readable `tag@repository-digest` matching the supplied tested local digest. Resolution of all five exact references is operator-attested but not durably accepted; publisher/signature/provenance, continuing availability, runtime health, and production acceptance remain blocked. |
| Image publisher/signature/provenance verification | blocked | No publisher identity, signature, attestation, or provenance was independently verified; see `docs/images.md`. |
| OpenBao corrected exact-path preflight | tested locally | Passed after cleanup without starting or contacting a service, creating files, or initializing/authenticating. |
| OpenBao sealed compatibility | bounded runtime pass | Fresh corrected-topology project was healthy at restart 0, independently sealed/uninitialized, CA-verified on loopback with HTTP 200, refused on a non-loopback host address, and fully torn down. No bootstrap or recovery material was generated or used. |
| OpenBao bootstrap, audit, identity, and secret access | blocked | Initialization, unseal, authentication, helper execution, and all bootstrap/later phases were not performed. |
| Zabbix core and synthetic-agent transport | independently reviewer-passed bounded runtime milestone | Four containers were healthy at restart 0; loopback web/API and server transport, API `7.0.14`, server-to-agent ping `1`, host-to-agent rejection, and absence of PostgreSQL/agent publication were recorded. No registration or monitoring acceptance is implied. |
| Live Zabbix API discovery/apply/export | blocked / not started | No live endpoint or identity was configured. Apply is hard-disabled and writes no receipt; no applicable plan creation exists until an identity-bound signed approval design is implemented and accepted. |
| Representative alert firing and recovery | not started | No target or service was exercised. |
| StackStorm notification-only receipt | blocked | Compose has no runnable StackStorm service or automation profile. Every route is schema-required to remain explicitly disabled; policy references are valid desired intent but cannot deliver. Framing/signature/replay exercises are contract/vector tests only. Replay identity is source identity plus event ID, so a re-signed event remains the same event through the declared retention interval. There is no handler, so all requirements remain declarations rather than runtime enforcement. |
| Idempotent live reconciliation and drift handling | not started | Requires live Zabbix state and approved identities. |
| Exact disposable PostgreSQL stream restore and selected aggregates | attested demonstration; not durably accepted | The operator and validator observed the exact second timestamped clean-option restore exit `0`, and a live/bounded source/destination selected-aggregate match at `203|392|17659|6718|7000000`. Uncommitted evidence and expired transient events prevent durable acceptance; the aggregate is not complete database equivalence. The first execution and direct-pipe/no-dump account are operator-recorded; reuse of one mutable destination leaves generalized repeatability blocked. |
| Restored-application startup | **FAILED / not accepted** | Technical checks were observed on a fresh isolated internal network, but the validator used unrestricted inspection and exposed synthetic environment fields in private output. This handling incident requires safe field-scoped revalidation; values are not repeated. |
| Encrypted backup and production recovery | blocked / not tested | The script validates PostgreSQL identifiers and line-oriented manifest metadata, uses fixed `setsid timeout` process-group bounds, PID/starttime/PGID-safe handled cleanup, Python `hashlib`, and fail-closed manual stale-lock adjudication including missing/partial metadata. It requires exact nonempty ciphertext-set membership, fsyncs around required Linux atomic no-replace publication, preserves every set, and rejects retention/prune overrides. Retirement is a separate protected operator procedure only after decryption and restore verification. Extended/default ACLs are not inspected or enforced; SIGKILL/power loss can leave stale state; fsync is not a universal durability guarantee; nonempty ciphertext and hashes do not prove decryptability or plaintext integrity. The set contains validated dry-run planning metadata, not a Zabbix configuration backup; a sanitized live export path is unimplemented/unverified and blocked. No encrypted artifact, protected decryption, age identity, manifest/offsite custody, roles/global objects, PITR, application startup from restored data, or production recovery was tested. |
| Production/security acceptance | blocked | Requires all runtime, image, identity, monitoring, and recovery evidence above. |

## Not Tested

- Host-environment pytest and host ShellCheck, image scanning, live authenticated Zabbix API reconciliation, registration/dashboard/data validation, drift adoption/rejection against live state, encrypted backup execution, OpenBao audit verification, roles/global objects, PITR, safely validated application startup from restored data, and production recovery. External yamllint and pinned-container ShellCheck for both `scripts/backup.sh` and all shell scripts passed. The tracked-only detect-secrets scan reported zero findings only after excluding lines with the generated plan's exact `integrity` key; no value was printed or inspected.

The project is **not operational**. Both disposable OpenBao projects are torn down and no further OpenBao runtime action is authorized. Any future protected bootstrap requires separate approval and recovery-material custody; it must not be inferred from this sealed compatibility milestone. Commissioning remains incomplete until blocked and not-started critical checks pass with evidence.
