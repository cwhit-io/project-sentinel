# Commissioning Report

Date: 2026-07-28

This is a sanitized local validation and disposable runtime report for a commissioning baseline. The project remains non-operational and not production-ready. Per [`docs/scope.md`](scope.md), PostgreSQL backup, OpenBao, and TLS termination are operator-managed and are not Sentinel's responsibility.

## Evidence Recorded

- `python3 scripts/sentinel.py validate`: inventory and policy schemas passed for the synthetic lab asset and the HTTP uptime asset without a `credentials` field.
- `python3 scripts/sentinel.py catalog`: deterministic catalog regenerated at `docs/monitoring-catalog.md`.
- `python3 scripts/sentinel.py plan --dry-run`: sanitized plan regenerated at `monitoring/exports/plan.json`; both `sentinel-lab-agent` and `bhm-org-uptime` are recorded.
- `env SENTINEL_NAMESPACE=sentinel-static COMPOSE_PROJECT_NAME=sentinel-static POSTGRES_USER=zabbix POSTGRES_PASSWORD=<synthetic> POSTGRES_DB=zabbix docker compose --env-file /dev/null --project-directory . -f compose.yaml --project-name sentinel-static config --quiet`: Compose syntax and interpolation passed with matching namespace/project semantics. The placeholder value was synthetic and was not stored.
- `python3 -m compileall -q automation scripts tests`: Python compilation passed.
- A tracked-file-only scan found no dev-root-token configuration, private-key material, or secret values in that limited scope. Expected `secret://` references are opaque inventory metadata and are excluded from plans; Sentinel no longer stores credentials in inventory.
- Disposable Zabbix core runtime: `postgres`, `zabbix-server`, and `zabbix-web` reached healthy container states; PostgreSQL readiness, the Zabbix server process, and internal web HTTP 200 were verified. No live apply or target registration occurred.
- Public ingress beta (Cloudflare): TLS terminates externally for `sentinel.bhm.li` and forwards plain HTTP to `10.10.97.18:18080`. Per `docs/scope.md`, this termination is operator-managed and is not a Sentinel responsibility.
- Mocked reconciliation v3 static evidence records separately invoked focused and full suites at **42** and **129 passed**, validation of one synthetic asset, compilation, diff checking, and a zero-match scoped evidence-file secret-pattern scan. See `docs/evidence/reconciliation-v3-static-20260728.md`.
- Protected-live gate static evidence covers bounded provider/transport/response/parse negatives, exact-type boundaries, internally derived target binding, fail-closed pre-directory Sentinel-only tag persistence, and exploding-probe approval/executor denials. See `docs/evidence/reconciliation-live-gate-static-20260728.md`.
- Mocked reconcile evidence covers full validate→normalize→discover→plan→sign→apply→verify→receipt, manual and auto sign paths, wrong/expired/missing signature negatives, missing-scope-tag negatives, missing private key, scope mismatch, closed HTTP inventory schema negatives, and the closed `FileCredentialProvider` owner/mode/symlink guards. The full pytest count is **150 passed**. See `docs/evidence/reconcile-20260728.md`.
- The sample inventory now describes only the disposable runtime identity `sentinel-lab-agent` at `synthetic-zabbix-agent`, with built-in template `Linux by Zabbix agent`, approved server template `Zabbix server health`, and no credentials field. The HTTP uptime inventory describes `bhm-org-uptime` against `https://blackhawkministries.org/` with a closed `http_checks` block, no templates, scope `public-uptime`, and host group `Sentinel external uptime`. The regenerated catalog and dry-run plan are review evidence only.

## Blocked
- Per `docs/scope.md`: OpenBao bootstrap, encrypted PostgreSQL backup, restore repeatability, restored-application startup, and production recovery are operator-managed concerns and are explicitly out of scope for Sentinel.
- Isolated dependency setup: host `pip`/`pytest` remain unavailable; disposable packages were installed outside the repository under `/tmp/opencode` and passed the suite there.
- Image authenticity/signature/provenance verification remains blocked. The supplied tested local repository digests are now immutable Compose pins but were not independently authenticated; see `docs/images.md`.
- Live Zabbix API discovery/apply/export is blocked. Apply is hard-disabled and writes no receipt; no applicable plan creation exists.

## Verification Matrix

| Area | Status | Evidence or gap |
|---|---|---|
| Inventory and policy schema validation | tested locally | Recorded validation passed for the lab asset and the HTTP uptime asset, both with no credentials field. |
| Deterministic catalog generation | tested locally | Recorded catalog generation passed. |
| Review-required dry-run plan and secret/path exclusion | tested locally | Recorded plan contains both assets and no credential references or absolute paths. |
| Reconciliation planner and apply guard | implemented/static-tested | All generated and verified artifacts are `dry-run`; no applicable format exists. Apply is unconditionally disabled before parsing, receipt generation, or adapter access, including with `--approve` and recomputed integrity. The mocked `sentinel reconcile` pipe is closed and evidence-backed; live apply is not authorized. |
| HTTP inventory schema and HTTP uptime plan | tested locally | `tests/test_http_inventory_schema.py` covers the closed `http_checks` contract; the plan emits `create_host`, `create_httptest`, and `create_item` for the HTTP asset. |
| Compose syntax and interpolation | tested locally | Recorded `docker compose config --quiet` passed with synthetic environment values. |
| Python compilation | tested locally | Recorded `compileall` passed. |
| Automated pytest suite | static pass | External dependency path pytest suite passes after the scope cleanup; sentinel validate, catalog, plan --dry-run, the new reconcile pipe, yamllint, and the bounded secret scan all pass. The full pytest count is **150 passed**. |
| Immutable image references and registry resolution | operator-attested point-in-time resolution / statically pinned | Every runnable image uses a readable `tag@repository-digest` matching the supplied tested local digest. Resolution is operator-attested but not durably accepted; publisher/signature/provenance, continuing availability, runtime health, and production acceptance remain blocked. See `docs/images.md`. |
| Zabbix core and synthetic-agent transport | independently reviewer-passed bounded runtime milestone | Four containers were healthy at restart 0; loopback web/API and server transport, API `7.0.14`, server-to-agent ping `1`, host-to-agent rejection, and absence of PostgreSQL/agent publication were recorded. No registration or monitoring acceptance is implied. |
| Local synthetic login beta | operator-attested login; independently checked transport | Project `sentinel-beta-20260728` has exactly four intended digest-pinned services healthy at restart 0. Web is available only at `127.0.0.1:18080`; server transport is available only at `127.0.0.1:11051`; PostgreSQL and the agent have no host publication. The user/operator attests successful frontend login; no credential was received or recorded. Authentication negative/security testing, registration, monitoring, and production acceptance remain untested. See `docs/evidence/beta-runtime-20260728.md`. |
| Public ingress beta (Cloudflare) | bounded operator note | A Cloudflare tunnel terminates TLS for `sentinel.bhm.li` and forwards plain HTTP to the local Zabbix web at `10.10.97.18:18080`. The web container is bound to the LAN address so the tunnel always has a deterministic non-loopback target. The Zabbix `Admin` password was changed by the operator before this URL was reachable. No agent recorded that value. This is a commissioning ingress only, not a production ingress. |
| Live Zabbix API discovery/apply/export | blocked / not started | No live endpoint or identity was configured. Apply is hard-disabled and writes no receipt; no applicable plan creation exists until an identity-bound signed approval design is implemented and accepted. The mocked reconcile pipe is mocked-only and cannot reach a real endpoint. |
| Representative alert firing and recovery | out of scope per `docs/scope.md` | Operator-managed; not a Sentinel milestone. |
| StackStorm notification-only receipt | blocked | Compose has no runnable StackStorm service or automation profile; per `docs/scope.md`, StackStorm is a trusted upstream platform. |
| Mocked reconciliation and drift handling | bounded static/mock pass; live blocked | Exact Zabbix API `7.0.14` is pinned. Bounded scope/name identity queries precede full normalization. Closed receipt/result fields and strict per-field validation are static. The separate client and v3 simulator remain inert exact fakes. The HTTP uptime asset exercises `httptest.get`, `item.get`, and the new `create_httptest` / `create_item` plan operations end-to-end inside the mocked pipe. No live state, mutation adapter, delete executor/auth, applicable artifact, or runtime acceptance exists. |
| Protected-live reconciliation gate | static/disposable read-only gate; execution blocked | HTTPS-default transport and literal-loopback commissioning HTTP were exercised only with a local stub. The exact read boundary allows only six discovery methods and derives its binding internally. Approval and execution deny before touching inputs; no available mutation policy, mutation client/write handle, credential CLI, or approval acceptance exists. The mocked apply path uses an exact `WriteZabbixClient` distinct from the read client and is wired only in tests. A first future protected ceremony requires exactly one opaque least-privilege read-token reference, never its value. A later write token and approval key require a separate design and authorization; no approval verification against a real operator is currently accepted. Independent review, validator rerun, exact runtime trust/compatibility checks, and descriptor-pinned artifact and filesystem-failure testing remain required. Current path/mode/fsync checks do not eliminate TOCTOU or prove crash/power-loss durability. Apply, mutation against a live endpoint, retries, receipt persistence against a live endpoint, and the retired UI runbook remain unavailable. `host.delete`, `httptest.delete`, and `item.delete` are present only as inert unavailable tombstone policies with `executor: none`; no delete executor is available. |
| PostgreSQL backup and production recovery | out of scope per `docs/scope.md` | Operator-managed concerns handled by an existing database install outside this repository. |
| Production/security acceptance | blocked | Requires all runtime, image, identity, monitoring, and recovery evidence above; the operator-managed items in `docs/scope.md` are not part of Sentinel acceptance. |

## Not Tested

- Host-environment pytest and host ShellCheck, image scanning, live authenticated Zabbix API reconciliation, registration/dashboard/data validation, drift adoption/rejection against live state, OpenBao audit verification, encrypted PostgreSQL backup execution, PostgreSQL restore repeatability, accepted restored-application startup, and production recovery. The post-cleanup external disposable suite passed under `/tmp/opencode/sentinel-python-tools` per `docs/scope.md`.

The local synthetic commissioning beta is operational only for bounded login testing; the project is **not production operational**. Commissioning for what Sentinel owns remains incomplete until the blocked and not-started checks pass with evidence; the operator-managed items in `docs/scope.md` are excluded from Sentinel acceptance.